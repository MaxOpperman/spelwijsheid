# Docker Compose file for local testing and production
version: '3.8'

services:
  spelwijsheid:
    build: .
    ports:
      - '3000:6749'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
